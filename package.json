{
  "//": "The core arrives as a REAL package: JSR's npm-compat layer materializes the full file tree under node_modules (runtime js/css/sprites, tools, schema), which /_rt serving, the build and the tests all read. `@1` = every 1.x release; deno.lock records what resolved, deno task install takes the newest.",
  "dependencies": {
    "@microspec/core": "npm:@jsr/microspec__core@1"
  }
}
